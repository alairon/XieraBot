const TokenManager = require('../../../../xiera/components/Core/Token/TokenManager');
const assert = require('chai').assert;

/* Config */
const testToken = 'test.';
const testFlags = 'imu';
const Test = new TokenManager(testToken, testFlags);

describe('Token and User Actions', () => {
  describe('User Actions', () => {
    it('Returns null', () => {
      assert.equal()
    })
  });
});